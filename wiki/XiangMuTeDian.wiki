# 为啥用TreapDB?

==特点==：

  # 支持大数据量（1亿以内的key-value pair，在SATA硬盘，64位服务器上8200tps写入，15000tps读取）
  # 不只支持GET和PUT操作，还支持取前缀，取区间（开区间、闭区间），K个最小值，K个最大值
  # 支持主从复制，从而实现读写分离和灾备
  # 同时支持Memcached的协议和基于Thrfit的协议（后者可以支持轻松生成各种语言的客户端）
  # 可以把TreapDB的索引数据结构嵌入自己的代码，就一个jar包，完全可以不用使用服务器